{% embed 'partials/base.html.twig' %}

	{% set base_url = page.parent.url %}
	{% set feed_url = base_url %}

	{% if base_url == '/' %}
		{% set base_url = '' %}
	{% endif %}

	{% if base_url == base_url_relative %}
		{% set feed_url = base_url~'/'~page.parent.slug %}
	{% endif  %}

	{% block content %}
		
		<div class="header single-item" style="background-image:url('{{ page.media|first.url('') }}');">
			<div class="container">
			{% if page.taxonomy.category %}
				<span class="tags">
					{% for category in page.taxonomy.category %}
						<a class="label label-primary" href="{{ base_url }}/category{{ config.system.param_sep }}{{ category }}">{{ category }}</a>
					{% endfor %}
				</span>
			{% endif %}
				<h1>{{ page.title }}</h1>
				{% if config.plugins.breadcrumbs.enabled %}
					{% include 'partials/breadcrumbs.html.twig' %}
				{% endif %}

			</div>
		</div>
		<div class="container">

			<div class="row">
				<div class="col-sm-8 blog-main">


					{% include 'partials/blog_item.html.twig' with {'blog':page.parent, 'truncate':false, 'big_header':true} %}
				</div>

				<div class="col-sm-3 col-sm-offset-1 blog-sidebar">
					{% include 'partials/sidebar.html.twig' with {'blog':page.parent}%}
				</div>
			</div>
		</div>
	{% endblock %}

{% endembed %}
